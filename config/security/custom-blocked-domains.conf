# WoW System - User Custom Blocked Domains (TIER 3)
# Add domains you want to block here
#
# These domains will be blocked in addition to system blocks
# in system-blocked-domains.conf
#
# Note: TIER 1 critical blocks (localhost, private IPs, metadata endpoints)
# are hardcoded and cannot be overridden
#
# Format: One domain per line
# Wildcards supported: *.example.com (matches subdomains)
#
# Examples:
# suspicious-site.com
# *.tracking-domain.net
# ads.example.com
#
# Add your blocked domains below:

