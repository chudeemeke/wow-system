# WoW System - Blocked Domains (TIER 2)
# System-level blocked domains
# Known malicious or problematic domains that should be blocked by default
#
# Note: TIER 1 critical security blocks (localhost, private IPs, etc.)
# are hardcoded in domain-lists.sh and cannot be modified
#
# Users can add additional blocks via custom-blocked-domains.conf

# Placeholder - add known malicious domains here as discovered
# example-malware.com
# phishing-site.net
